# Playback
h              seek -5           # left = rewind 5s
l              seek 5            # right = forward 5s
j              seek -60          # down = back 1m
k              seek 60           # up = forward 1m
